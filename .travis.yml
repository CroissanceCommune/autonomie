language: python
python:
  - 2.7
  - 3.3
services:
    - mysql
install: pip install -r requirements.txt -r test_requirements.txt .
script:  py.test
before_install:
  - ./.travis_before_install.sh
notifications:
    irc:
        channels:
            - "irc.freenode.org#majerteam"
        on_success: change
