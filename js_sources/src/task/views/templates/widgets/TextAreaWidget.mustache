{{#if editable }}
    <label for='{{field_name}}'>{{ title }}</label>
    <textarea name='{{field_name}}' class='form-control' rows='{{ rows }}'>{{ value }}</textarea>
{{else}}
    {{ value }}
{{/if}}
